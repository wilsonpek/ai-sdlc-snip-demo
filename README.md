# snip-bundle — Generated Output

**Do not hand-edit this branch.**

All content is produced by [`scripts/build-bundle.mjs`](https://github.com/wilsonpek/ai-sdlc-snip-demo/blob/main/scripts/build-bundle.mjs)
in the `main` branch, which assembles the Bun backend, Angular SPA, and Node CLI
into a single self-contained deployment package.

To regenerate and publish:

```sh
node scripts/build-bundle.mjs --push
```
